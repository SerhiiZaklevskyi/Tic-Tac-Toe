// eslint-disable-next-line consistent-return
const checkComboO = (value1, value2, value3) => {
  if (value1 === "O" && value2 === "O" && value3 === "O") {
    return true;
  }
};

export default checkComboO;
