// eslint-disable-next-line consistent-return
const checkComboX = (value1, value2, value3) => {
  if (value1 === "X" && value2 === "X" && value3 === "X") {
    return true;
  }
};

export default checkComboX;
